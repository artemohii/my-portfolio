$('.owl-carousel').owlCarousel({
    items:1,
    lazyLoad:true,
    loop:true,
    margin:10
});
/* function carouselFn(){
    if ($(window).width() < 500){
       $(".owl-carousel").owlCarousel({
   
        items:1,
        lazyLoad:true,
        margin:10,
        loop:true,
        });
     }else if ($(window).width() < 992){
        $(".owl-carousel").owlCarousel({
   
            items:1.5,
            lazyLoad:true,
            margin:10,
            loop:true,
            });

     }
     else{
      $(".owl-carousel").owlCarousel({
   
       items:3.5,
       lazyLoad:true,
       margin:10,
       loop:true,
      });
     }
    };
   
    $(document).ready(function() {
        carouselFn();
   });
   $(window).resize(function(){
        carouselFn();     
    }); */
    